

=面试
写strcpy函数
考察
-编程风格
-出错处理
-算法复杂度分析——用于提高性能

=软件工程要解决的核心问题
-提高质量和生产率

=质量属性
-正确性、健壮性、可靠性、效率、易用性、可读性、可扩展性、可复用性、兼容性、可移植性

=
-指针、内存管理

=相关书籍
-程序设计方面

=后续阅读
-参考文献

#头文件
=======================================================================================
* ifndef/define/endif_防止头文件被重复引用_
* #include<filename.h>_引用标准库的头文件_
* #include"filename.h"_引用非标准库的头文件_
+ 头文件中只放声明不放定义.
	C++语法中，类的成员函数可以在声明的同时被定义，并且自动成为内联函数。
	这虽然会带来书写上的方便，但却造成了风格不一致，弊大于利。
	建议将成员函数的定义与声明分开，不论该函数体有多么小。
+ 不提倡使用全局变量，尽量不要在头文件中出现像extern int value这类声明

头文件作用
----------------------
1. 通过头文件来调用库功能。在很多场合，源代码不方便（或不准）向用户公布，只要向用户提供
   头文件和二进制的库即可。用户只需要按照头文件中的接口声明来调用库功能，而不必关心接口
   怎么实现。编译器会从库中提取相应的代码。
2. 头文件能加强类型安全检查。如果某个接口被实现或被使用时，其方式与头文件中的声明不一致，
   编译器就会指出错误。这一简单的规则能大大减轻程序员调试、改错的负担。

#目录结构
======================================================================================
* 如果一个软件的头文件数目比较多，通常应将头文件和定义文件分别保存于不同的目录，以便于
  维护。（如超过10个）
  例如头文件保存于include目录，定义文件保存于source目录（可以是多级目录）
* 如果某些头文件是私有的，他不会被用户的程序直接引用，则没有必要公开其声明。为了加强
  信息隐藏，这些私有的头文件可以和定义文件存放于同一个目录。


# 程序的版式
=======================================================================================
- 影响可读性
- 追求清晰、美观

## 空行
	* 每个类声明之后、每个函数定义结束之后都要加空行。
	* 在一个函数体内，逻辑上密切相关的语句之间不加空行，其他地方应加空行分隔。
## 代码行
	* 一行代码只做一件事，如只定义一个变量，或只写一条语句。这样打代码容易阅读，并且方便于写注释。
	* if/for/while/do等语句自占一行，执行语句不得紧跟其后。不论执行语句有多少都要加{ }.这样可以防止书写失误。
	+ 尽可能在定义变量的同时初始化该变量（就近原则）
	  如果引用了未被初始化的变量，可能会导致程序错误。本建议可以减少隐患。
## 代码行内的空格
	* 关键字之后要留空格。const、virtual、inline、case等关键字后留空格，否则
	  无法辨析关键字。_if、for、while等关键字后留一个空格后再跟左括号，以突出关键字。_
	* 函数名之后不留空格，紧跟左括号，以与关键字区别。
	* “(” 向右紧跟；“)”、“,”、“;”向前紧跟，紧跟处不留空格。
	* 前紧跟符号后边留空格，后紧跟符号前边留空格（函数名后不留空格）
	* 二元运算操作符前后留空格。=、+=、>=、<=、 ==、 %、 +、 &&、 ||、 <<、 ^
	* 一元运算操作符前后不加空。!、~、++、 --、 &
	* ‘[]’、‘.’、‘->’操作符前后不加空
	* 对于表达式比较长的for语句和if语句，为了紧凑起见可以适当地去掉一些空格。
	  for (i=0; i<10; i++)
	  if ((a<=b) && (c<=d))
## 对齐
	* 程序分界符应独占一行并位于同一列，同时与引用它们的语句左对齐。
	* { }之内代码缩进

## 长行拆分
	* 代码行最大长度控制在70至80字符。
	* 长表达式要在优先级操作符处拆分成新行，操作符放在新行之首（以便突出操作符）。
	  拆分出的新行要进行适当的缩进，使排版整齐，语句可读。

## 修饰符的位置
	* \*、&紧靠变量名

## 注释

	* 注释是对代码的提示，而不是文档。注释的花样要少。
	* 边写代码边注释，修改代码同时修改相应的注释，以保证注释与代码的一致性。不再有用的注释要删除。
	* 注释应当准确、易懂，防止注释有二义性。
	* 尽量避免注释中使用缩写，特别是不常用的缩写
	* 注释的位置应与被描述的代码相邻，可以放在代码的上方或者右方，不可放在下方。
	* 当代码比较长，特别是有多重嵌套时，应当在一些段落的结束处加注释，便于阅读。 
注释作用
--------------------
1. 版本、版权声明
2. 函数接口说明
3. 重要代码行或段落提示

page20
